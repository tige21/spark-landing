---
import Section from '../layout/Section.astro';
import Reveal from '../ui/Reveal.tsx';
import Parallax from '../ui/Parallax.tsx';
import DecksShelf from './DecksShelf.tsx';
import { Image } from 'astro:assets';
import { engraving } from '../../assets/images';

interface Props {
  headline: string;
  caption: string;
  lang: 'ru' | 'en';
}
const { headline, caption, lang } = Astro.props;
---

<Section id="decks" class="decks">
  <div class="crest-clip" aria-hidden="true">
    <div class="crest-holder">
      <Parallax client:visible distance={0} rotate={8}>
        <Image src={engraving('bg-decks')} alt="" class="crest" loading="lazy" widths={[600, 1000]} sizes="900px" />
      </Parallax>
    </div>
  </div>
  <div class="decks-head">
    <Reveal client:visible className="decks-enter">
      <h2 class="t-h1">{headline}</h2>
      <p class="t-lead caption">{caption}</p>
    </Reveal>
  </div>
  <DecksShelf lang={lang} client:visible />
</Section>

<style>
  .decks {
    position: relative;
    text-align: center;
    overflow: hidden;
  }
  /* Confine the engraving to the cards region (below the heading) and clip it
     there, so it backs the deck grid instead of the title and never spills. */
  .crest-clip {
    position: absolute;
    top: clamp(220px, 24vw, 360px);
    left: 0;
    right: 0;
    bottom: 0;
    overflow: hidden;
    z-index: var(--z-bg);
    pointer-events: none;
  }
  .crest-holder {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: min(520px, 56%);
  }
  .crest {
    width: 100%;
    height: auto;
    opacity: 0.12;
    display: block;
  }
  .decks-head {
    position: relative;
    z-index: var(--z-content);
    text-align: center;
  }
  .caption {
    max-width: 32ch;
    margin-inline: auto;
  }
</style>

<style is:global>
  /* Stronger, clearly-visible entrance for the "Девять колод" heading right as
     the block crosses in from the phone section — a longer fade + bigger rise
     than the default .reveal. Scoped to .decks-enter so other reveals are
     untouched; `:not(.is-in)` keeps it from fighting the global is-in reset. */
  .reveal.decks-enter {
    transition-duration: 0.7s;
  }
  .reveal.decks-enter:not(.is-in) {
    transform: translateY(34px);
  }
  .deck-grid {
    position: relative;
    z-index: var(--z-content);
    list-style: none;
    margin: var(--space-2xl) 0 0;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xl);
    perspective: 1100px;
  }
  .deck-cell {
    list-style: none;
    perspective: 700px;
    opacity: 0;
    transform: translateY(22px) rotateX(20deg);
    transform-origin: 50% 100%;
    transition: opacity 0.5s var(--ease-out), transform 0.5s var(--ease-out);
    transition-delay: calc(var(--i, 0) * 0.05s);
  }
  .deck-grid.is-in .deck-cell {
    opacity: 1;
    transform: none;
  }
  .deck-card {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    gap: var(--space-sm);
    padding: var(--space-xl);
    background: var(--paper);
    border: 1px solid var(--hairline);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-card);
    text-align: center;
    cursor: pointer;
    transition: box-shadow var(--dur-normal) var(--ease-out),
      border-color var(--dur-normal) var(--ease-out),
      transform var(--dur-normal) var(--ease-out);
  }
  .deck-card:hover,
  .deck-card:active,
  .deck-card.is-tapped {
    box-shadow: var(--shadow-float);
    border-color: color-mix(in srgb, var(--wine) 35%, var(--hairline));
    transform: translateY(-8px) rotateX(-6deg);
  }
  @media (prefers-reduced-motion: reduce) {
    .deck-cell { opacity: 1 !important; transform: none !important; transition: none !important; }
    .deck-card { transition: none !important; }
    .deck-card:hover,
    .deck-card.is-tapped { transform: none !important; }
  }
  .deck-card img {
    width: 110px;
    height: 110px;
  }
  .deck-name {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 1.1rem;
    color: var(--ink);
    margin-top: var(--space-xs);
  }
  .deck-sample {
    font-family: var(--font-serif);
    font-size: 0.95rem;
    line-height: 1.4;
    color: var(--meta);
    max-width: 26ch;
  }
  @media (max-width: 860px) {
    .deck-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 520px) {
    .deck-grid {
      grid-template-columns: 1fr;
      max-width: 340px;
      margin-inline: auto;
    }
  }
</style>
