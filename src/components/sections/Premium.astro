---
import Section from '../layout/Section.astro';
import Reveal from '../ui/Reveal.tsx';
import { Image } from 'astro:assets';
import { engraving } from '../../assets/images';

interface Props {
  headline: string;
  body: string;
}
const { headline, body } = Astro.props;
---

<Section class="premium">
  <div class="grid">
    <Reveal client:visible delay={0.1} class="art">
      <Image src={engraving('paywall-crest')} alt="" widths={[320, 560]} sizes="(max-width: 800px) 70vw, 420px" loading="lazy" />
    </Reveal>
    <Reveal client:visible class="copy">
      <h2 class="t-h1">{headline}</h2>
      <p class="t-lead">{body}</p>
    </Reveal>
  </div>
</Section>

<style>
  .grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: var(--space-3xl);
  }
  .copy :global(.t-lead) {
    margin-top: var(--space-md);
  }
  .art :global(img) {
    width: 100%;
    height: auto;
    max-width: 420px;
    margin-inline: auto;
    display: block;
  }
  @media (max-width: 800px) {
    .grid {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .art {
      order: -1;
    }
  }
</style>
