# Витиеватый коннектор How + ранний reveal на мобиле

**Проект:** spark-landing · **Ветка:** feature/landing-build · **Дата:** 2026-06-16
**Тип:** UI-полировка (mobile)

## Settings
- Тесты-как-код: нет. Проверка — Playwright 390px.
- Деплой: оба зеркала через `scripts/deploy.sh`.

## Контекст (из кода)
- `PostalRouteVertical.tsx` — сейчас CSS-линия (`.route-v` 2px по центру + fill по `--p`). Идёт прямо по тексту.
- `How.astro` `.route-band-v` — width 2px, центр колонки, `@media (max-width:480)`.
- `in-view.ts` — IO `rootMargin: '0px 0px -10% 0px'` (отрицательный → reveal поздно), threshold 0.05.
- Горизонтальный `PostalRoute.tsx` — пример SVG scroll-draw (stroke-dashoffset + getPointAtLength для точки).

## Tasks

- [x] **W1 — Витиеватый (serpentine) вертикальный коннектор.**
  Переделать `PostalRouteVertical.tsx` из CSS-линии в **SVG с вертикальным S-волновым путём** (транспонировать горизонтальный `PostalRoute`: напр. `M70,20 C18,200 122,360 70,540 S18,860 70,1000` в viewBox ~`0 0 140 1020`, `preserveAspectRatio="none"`): дуга влево→вправо→влево→вправо. Рисование по скроллу через `stroke-dasharray/strokeDashoffset` (фоновый бледный путь + wine-«нарисованная» часть) + едущая wine-точка по `getPointAtLength`. Reduced-motion: путь нарисован полностью, без точки.
  `How.astro`: `.route-band-v` расширить на ширину колонки (не 2px) — `left/right` по краям колонки, чтобы волна имела горизонтальный размах; путь весит позади центрированных карточек/текста и **огибает текст**, а не идёт сквозь. Обновить `is:global` стили под SVG (классы из острова). z-index 0 (за шагами).

- [x] **W2 — Reveal раньше на мобиле (и везде).**
  `src/lib/in-view.ts`: `rootMargin` `'0px 0px -10% 0px'` → `'0px 0px 15% 0px'` (положительный нижний → зона расширяется вниз, элемент проявляется раньше, до полного входа в экран). threshold оставить 0.05 (или 0). Фикс «Девять колод и ниже появляются поздно».

- [x] **W3 — Проверка Playwright (390px) + деплой.**
  390: коннектор — волна (влево/вправо), не идёт по тексту, рисуется и точка едет при скролле; «Девять колод» и блоки ниже проявляются раньше (не в упор). Десктоп не сломан (горизонтальный route как был). Консоль чистая. `bash scripts/deploy.sh` на оба зеркала.

## Порядок
W1 → W2 → W3.
