# Полировка по ревью: контраст подзаголовка Hero + декор-гравюра в What

**Проект:** spark-landing · **Ветка:** feature/landing-build (НЕ создаём) · **Дата:** 2026-06-19
**Тип:** UX-полировка (по ревью лендинга, БЕЗ пункта 1 — длину телефона не трогаем)

## Settings
- Тесты-как-код: нет. Проверка — Playwright 390 + 1440.
- Логирование: нет (CSS/контент).
- Docs: нет.
- Деплой: оба зеркала `scripts/deploy.sh`.

## Roadmap Linkage
Milestone: "none" · Rationale: точечная полировка.

## Контекст (из ревью + кода)
- **#2 (Hero subtitle):** `.hero-content .subtitle` = `color: var(--meta-strong)` + text-shadow-ореол; поверх гравюры пресса на пергаменте читается бледно. Затемнить.
- **#3 (What decor):** `What.astro` `SectionDecor` слой `postmark` (top:10%, right:-3%, width 210, opacity 0.08). Изолированный топ What чистый; но на стыке Hero→What бледная гравюра может проглядывать над заголовком и читаться как случайное пятно. Опустить/уменьшить/убрать слой, чтобы над заголовком было чисто (как crest в Decks бэкает сетку, а не заголовок).
- Пункт 1 ревью (длина секции телефона) — НЕ трогаем по решению пользователя.

## Tasks

- [x] **PR-1 — Hero: контраст подзаголовка.**
  `HeroScroll.css` `.hero-content .subtitle`: `--meta-strong` → `var(--ink)` (или очень тёмный), при необходимости усилить text-shadow-ореол для читаемости поверх гравюры. Размер/позицию не трогать.

- [x] **PR-2 — What: декор-гравюра на стыке/сверху.**
  `What.astro` SectionDecor `postmark`-слой: опустить (top ниже, к области пиллз/карточек) / уменьшить width|opacity / либо убрать — чтобы над заголовком «Это не игра в карты…» ничего стороннего не выглядывало на стыке Hero→What. `spark-mark` снизу можно оставить. Проверить десктоп+мобайл.

- [x] **PR-3 — Проверка (390+1440) + деплой.**
  Подзаголовок Hero читается чётко; топ What и стык Hero→What чистые; остальное не сломано; консоль чистая; reduced-motion ок. `bash scripts/deploy.sh` оба зеркала.

## Commit Plan
- **C1** (PR-1 + PR-2): `fix(landing): hero subtitle contrast + tidy What decor at seam`
- Деплой после PR-3.

## Порядок / зависимости
PR-1 ∥ PR-2 → PR-3.

## Открытые вопросы / риски
1. Если затемнение подзаголовка до `--ink` сделает его «вторым заголовком» (слишком жирно по тону) — взять промежуточный тёмный, но контраст приоритетнее.
2. `postmark`-слой может быть намеренной частью композиции; не убирать совсем, если простой сдвиг/уменьшение решает «проглядывание над заголовком».
