# iPhone scroll-scrub блок + ровный коннектор How + выравнивание карточек

**Проект:** spark-landing · **Ветка:** feature/landing-build (новую не создаём) · **Дата:** 2026-06-16
**Тип:** feature (iPhone scroll-scrub) + UI-полировка

## Settings
- Тесты-как-код: нет (UI/визуал). Проверка — Playwright 390px + 1440px.
- Docs: нет.
- Деплой: оба зеркала через `scripts/deploy.sh`.

## Контекст (из кода)
- Коннектор `PostalRouteVertical.tsx` — сейчас размашистый асимметричный серпантин; `How.astro` `.route-band-v` (≤480).
- Карточки шагов `.art` — исходники `onb-postoffice/onb-cards/onb-talk.png` 470×470, `seal-approved.png` 640×640 (все квадратные); неровность — в разных внутренних рамках/полях гравюр.
- Секция **`game`** (`ru.json`: «Одна карта — и стол замолкает по-хорошему» / «Вот как это выглядит в приложении») — **НЕ используется** в `index.astro`. Кандидат для iPhone-блока.
- Готовая инфраструктура scroll-scrub: `CanvasSequence.tsx`, `useScrollScene`, `Parallax.tsx`, `Scene3D/Layer`, пайплайн кадров (`public/hero-frames` + manifest).

## Tasks

- [x] **G1 — Коннектор How: ровный симметричный серпантин (узлы по центру карточек).**
  Переделать `PostalRouteVertical.tsx`: вместо размашистого серпантина — **аккуратная линия строго по вертикальному центру колонки**, проходящая через центры карточек (узлы — в центрах арт). Вариант по умолчанию: прямая центральная вертикаль (чисто, симметрично); опционально — очень мягкая СИММЕТРИЧНАЯ волна с малой амплитудой и узлами точно в центрах карточек (без захода на текст по краям). Scroll-draw + точка сохранить. `How.astro`: вернуть узкую центрированную полосу. Reduced-motion — статично. Показать пользователю прямую vs мягкую волну.

- [x] **G2 — Выровнять карточки-картинки шагов (trim+центрирование, рамки симметричны).**
  Осмотреть 4 гравюры. Нормализовать к единому виду через ImageMagick (как кадры героя): привести рамку/поля к одинаковым — напр. `-trim` до контента → `-resize` в общий квадрат → `-bordercolor white -border N`/`-extent` с одинаковым полем; одинаковый итоговый размер. Бэкап оригиналов. Цель — три квадратные карточки выглядят идентично по рамке; `seal-approved` (круглый медальон, 4-й шаг) — отдельный стиль, не трогаем рамку. Альтернатива/дополнение — CSS-рамка на `.art`.

- [x] **G3 — НОВОЕ: секция «как в приложении» с iPhone scroll-scrub.** ✅
  Реализовано: `AppPreview.astro/.tsx/.css` (между How и Decks в `index.astro`), iPhone-мокап (CSS-рамка + dynamic island), внутри экрана `CanvasSequence` со scroll-scrub, parallax-искры (Scene3D/Layer), pinned ~1.3vp как у героя. Кадры сценария **записаны самостоятельно**: Playwright-скрипт (`/tmp/cap-app.cjs`) прогоняет живой staging-апп (cards-staging) с инъекцией localStorage (skip онбординг/согласие/тултипы), снимает 8 реальных карт (dsf3, ru-locale) → `public/hero-frames/app/*.webp` (720w, ~212K) + manifest. Морфинг-дизолв отверг (гостинг текста), оставлены чистые hard-cuts. Проверено Playwright 1440+390: pinned, карты сменяются (1/40→5/40), консоль чистая. Кадры в `.gitignore` (как hero-frames) — едут через dist/.
  Отрендерить неиспользуемую `game`-секцию как новый блок **между How и Decks**: iPhone-мокап (CSS/SVG-рамка с экраном), внутри экрана — `CanvasSequence` со scroll-scrub реального сценария приложения (старт → проигрывание карточек), + лёгкий parallax (Scene3D/Layer) как у героя. Переиспользовать `useScrollScene`/`CanvasSequence`/`Parallax`. Заголовок/подпись из `game`. Десктоп — iPhone сбоку + текст рядом; мобайл — iPhone по центру, текст сверху.
  **⚠️ БЛОКЕР (ассет):** нужны кадры сценария приложения — запись экрана (QuickTime/телефон) → извлечь кадры (`ffmpeg`/ezgif) → `public/app-frames/` + `manifest.json` (пайплайн как hero-frames). Без записи доделать нельзя. Запросить запись у пользователя; до неё — сверстать секцию со статичным постером-заглушкой и заложить wiring под кадры.
  Подзадачи: (a) `index.astro` + новый `AppPreview.astro` (iPhone-рамка + scroll-scrub-скелет, постер-заглушка); (b) после получения записи — извлечь кадры, manifest, подключить, настроить scrub/pin; (c) проверка.

- [~] **G4 — Проверка Playwright (390+1440) + деплой.** (проверка ✅ локально; деплой — ждёт подтверждения)
  Коннектор ровный по центру; карточки выровнены; iPhone-секция вёрстка ок (со заглушкой или кадрами), parallax/scrub при скролле; консоль чистая. `bash scripts/deploy.sh` на оба зеркала.

## Commit Plan
- **C1** (G1, G2): `fix(landing): center how-connector + normalize step card frames`
- **C2** (G3a): `feat(landing): app-preview iphone section scaffold (scroll-scrub ready)`
- **C3** (G3b): `feat(landing): wire real app scenario frames into iphone scrub` (после ассета)
- Деплой после G4.

## Порядок / зависимости
G1 → G2 → G3a (скелет, без блокера) → [ассет: запись экрана] → G3b → G4.
G3b блокируется получением записи от пользователя; G1/G2/G3a/деплой можно сделать сразу.

## Открытые вопросы (для пользователя)
1. Коннектор: прямая центральная линия или мягкая симметричная волна? (покажу оба).
2. iPhone-секция: запись сценария приложения — сможешь записать экран (старт → выбор колоды → вытягивание/ответ на карты)? В каком виде пришлёшь (mp4/mov/gif)?
3. Размещение iPhone-блока: между «Как это работает» и «Колоды» — ок?
