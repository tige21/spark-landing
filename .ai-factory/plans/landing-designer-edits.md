# Батч правок от дизайнера по лендингу (без пункта про длину телефона)

**Проект:** spark-landing · **Ветка:** feature/landing-build (НЕ создаём) · **Дата:** 2026-06-19
**Тип:** UX/визуальные правки по фидбеку дизайнера

## Settings
- Тесты-как-код: нет. Проверка — Playwright 390 + 1440.
- Логирование: минимальное (`DEBUG_SCROLL` где есть).
- Docs: нет.
- Деплой: оба зеркала `scripts/deploy.sh`.

## Решения пользователя (по уточнению)
- #7: «ноль кринжа» → **«ноль фальши»**.
- #8: разделитель CTA → **запятая** + убрать точку, заголовок по центру.
- #1: асимметрия hero — десктоп (мобайл центр оставляем).

## Tasks

- [x] **DE-1 — Hero: пресс вправо, текст влево (десктоп ≥761).**
  `HeroScroll.css`: `.hero-bg` `object-position:right`; `.hero-content` left-align + ограничить ширину + прижать влево; `.hero-content::before`/`.hero-scrim` сместить центр градиента влево (~28%); eyebrow/cta по левому краю. Мобайл — без изменений.

- [x] **DE-2 — What: flip-карточки + 18+ гейт.**
  Новый остров `WhatCards.tsx` (client:visible): клик → 3D-флип, сзади пример вопроса; «Только 18+» → сначала подтверждение на карточке (18 + правила), потом вопрос. `What.astro` → `<WhatCards cards={…}/>`; `ru.json what.cards[]` (label/icon/sample, у 18+ gated+gate-копия). Reduced-motion — мгновенно, без 3D. Без эмодзи.

- [x] **DE-3 — ThroughPhone десктоп: один wheel = одно действие + зазор/выравнивание.**
  (#3) Дебаунс wheel-step: один непрерывный флик = один шаг (снимать lock только после паузы в wheel-событиях ≥~120–160мс + завершения доводки). (#5) Поджать зазор «Вот как это в приложении»→контент и выровнять копию сбоку по вертикали. Снап/мобайл не трогать.

- [x] **DE-4 — How: подписи шагов на одной базовой линии.**
  `.step h3` фикс. высота под 2 строки (min-height ~2.4em, align), чтобы body всех шагов начинался на одной линии (сейчас 2-строчный «Узнайте друг друга заново» сдвигает body). Десктоп/планшет/мобайл.

- [x] **DE-5 — Копия.**
  `ru.json`: «Напечатаем колоду под вас» → «Соберём колоду под вас» (×2, create/print); `premium.headline` «ноль кринжа» → «ноль фальши»; `cta.headline` → «Открой, и сегодня вечером разговор будет другим» (запятая, без точки). `FinalCta.astro`: явный `text-align:center` заголовку. Бренд «Печатня»/футер — НЕ трогаем. ПРИМ: кадры визарда («Бланк заказа», «Запечатать») — на стороне приложения.

- [ ] **DE-6 — Проверка (390+1440) + деплой.**
  Все 5 правок визуально ок, консоль чистая, reduced-motion ок, снап/мобайл целы. `bash scripts/deploy.sh` оба зеркала. Реальный флик/флип — на телефоне.

## Commit Plan
- **C1** (DE-1): `feat(landing): hero — press right, copy left on desktop`
- **C2** (DE-2): `feat(landing): What — flip cards with sample question + 18+ gate`
- **C3** (DE-3): `fix(landing): through-phone desktop — one wheel per action + tighten header gap`
- **C4** (DE-4 + DE-5): `fix(landing): How step baseline + copy (соберём / ноль фальши / CTA)`
- Деплой после DE-6.

## Порядок / зависимости
DE-1 ∥ DE-2 ∥ DE-4 ∥ DE-5 (независимы) · DE-3 (ThroughPhone) · → DE-6.

## НЕ в этом плане
- Длина секции телефона (~13 действий) — по решению пользователя не трогаем.

## Открытые вопросы / риски
1. **DE-2** — самый объёмный (новый интерактивный остров + гейт). Примеры вопросов — черновик в ru.json, легко поправить. 18+ гейт — маркетинговая «подтверждалка» на карточке, не юридическая.
2. **#6** на стороне лендинга меняем только тексты; кадры визарда (скрины приложения) останутся со словами «Бланк заказа/Запечатать» — отдельный разговор по приложению.
3. **DE-1** — асимметрия может потребовать тюнинга скрима/подложки, чтобы текст слева читался поверх смещённого пресса; перепроверка визуала обязательна.
4. **DE-3 (#3)** — реальная трекпад-инерция Playwright не воспроизводит точь-в-точь; финально проверить на ПК.
